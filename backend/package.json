{
  "name": "healthcare-backend",
  "version": "1.0.0",
  "description": "Healthcare Management System Backend",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test:coverage": "jest --coverage",
    "lint": "eslint src/ --ext .js",
    "lint:fix": "eslint src/ --ext .js --fix",
    "build": "echo 'No build step required for Node.js'",
    "test": "jest --testTimeout=30000 --forceExit --detectOpenHandles",
    "test:watch": "jest --watch --testTimeout=30000 --forceExit --detectOpenHandles"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "helmet": "^7.2.0",
    "mongoose": "^8.19.1"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "mongodb-memory-server": "^10.2.3",
    "nodemon": "^3.0.1",
    "supertest": "^6.0.0"
  },
  "keywords": [
    "healthcare",
    "medical",
    "api"
  ],
  "author": "Your Name",
  "license": "MIT"
}
